# 后端 API 地址
# 本地开发使用默认值即可
# 部署到服务器时，修改为实际的后端地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# 示例（部署时）：
# NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com/api
# 或者同域不同端口：
# NEXT_PUBLIC_API_BASE_URL=https://your-domain.com:3000/api
